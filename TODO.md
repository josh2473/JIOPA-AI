# TODO

- [x] Add mobile (≤700px) responsive overrides for the cinematic layout so the AI avatar remains visible (shrink/reposition `#cin-avatar-img-wrap`, reduce `#cin-hair` width, adjust `#cin-left`/`#cin-right`).


