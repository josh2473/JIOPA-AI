# TODO

- [x] Add mobile (≤700px) responsive overrides for the cinematic layout so the AI
      avatar remains visible — done in `design.css` under the phone media query.
- [x] Replace the eleven independent requestAnimationFrame loops with the single
      scheduler in `motion.js`.
- [x] Rebuild the seven science demos as labelled lessons (`explainers.js`).
- [ ] Add the Twi national anthem lyrics to `ANTHEM_DATA.twi.lines` in config.js.
- [ ] Add the JIOPA school anthem lyrics to `ANTHEM_DATA.school.lines`.
- [ ] Replace the ten placeholder gallery captions (photo12–photo21) with real ones.
- [ ] Record and add the missing `video/special-4.mp4` … `special-12.mp4`, or remove
      their placeholder trigger entries from `TAKEOVER_TRIGGERS`.
